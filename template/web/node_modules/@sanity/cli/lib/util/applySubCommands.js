"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

const applySubCommand = (yargs, cmd) => {
  if (cmd.handler || !cmd.action) {
    throw new Error('Subcommands must not have a `handler`, and must have an `action`');
  }

  return yargs.command(cmd);
};

const applySubCommands = subCommands => yargs => subCommands.reduce(applySubCommand, yargs);

var _default = applySubCommands;
exports.default = _default;