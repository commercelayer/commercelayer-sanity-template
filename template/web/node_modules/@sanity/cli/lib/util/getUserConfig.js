"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _configstore = _interopRequireDefault(require("configstore"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const sanityEnv = (process.env.SANITY_INTERNAL_ENV || '').toLowerCase(); // eslint-disable-line no-process-env

const defaults = {};
let config = null;

const getUserConfig = () => {
  if (!config) {
    config = new _configstore.default(sanityEnv && sanityEnv !== 'production' ? `sanity-${sanityEnv}` : 'sanity', defaults, {
      globalConfigPath: true
    });
  }

  return config;
};

var _default = getUserConfig;
exports.default = _default;