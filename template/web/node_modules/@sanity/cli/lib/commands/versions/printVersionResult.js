"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.printResult = printResult;
exports.getFormatters = getFormatters;
exports.default = void 0;

var _chalk = _interopRequireDefault(require("chalk"));

var _lodash = require("lodash");

var _findSanityModuleVersions = _interopRequireDefault(require("../../actions/versions/findSanityModuleVersions"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var _default = async (args, context) => {
  printResult(await (0, _findSanityModuleVersions.default)(context), context.output.print);
};

exports.default = _default;

function printResult(versions, print) {
  const {
    versionLength,
    formatName
  } = getFormatters(versions);
  versions.forEach(mod => {
    const version = (0, _lodash.padStart)(mod.version, versionLength);
    const latest = mod.version === mod.latest ? _chalk.default.green('(up to date)') : `(latest: ${_chalk.default.yellow(mod.latest)})`;
    print(`${formatName(mod.name)} ${version} ${latest}`);
  });
}

function getFormatters(versions) {
  const nameLength = versions.reduce(longestProp('name'), 0);
  const versionLength = versions.reduce(longestProp('version'), 0);

  const formatName = name => (0, _lodash.padEnd)(name, nameLength + 1).replace(/^@sanity\/(.*)/, `${_chalk.default.yellow('@sanity/')}${_chalk.default.cyan('$1')}`);

  return {
    nameLength,
    versionLength,
    formatName
  };
}

function longestProp(prop) {
  return (max, obj) => Math.max(max, obj[prop].length);
}