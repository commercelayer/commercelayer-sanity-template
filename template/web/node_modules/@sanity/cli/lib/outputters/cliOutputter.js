"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _ora = _interopRequireDefault(require("ora"));

var _chalk = _interopRequireDefault(require("chalk"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/* eslint-disable no-console */
let isFirstClear = true;
var _default = {
  print(...args) {
    console.log(...args);
  },

  warn(...args) {
    console.warn(...args);
  },

  error(...args) {
    if (args[0] instanceof Error) {
      console.error(_chalk.default.red(args[0].stack));
    } else {
      console.error(...args);
    }
  },

  clear: () => {
    // On first run, clear completely so it doesn't show half screen on Windows.
    // On next runs, use a different sequence that properly scrolls back.
    process.stdout.write(isFirstClear ? '\x1bc' : '\x1b[2J\x1b[0f');
    isFirstClear = false;
  },

  spinner(...args) {
    return (0, _ora.default)(...args);
  }

};
exports.default = _default;