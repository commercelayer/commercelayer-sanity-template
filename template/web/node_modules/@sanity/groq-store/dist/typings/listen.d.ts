import { Subscription, MutationEvent, Config } from './types';
export declare function listen(EventSourceImpl: typeof EventSource, config: Config, handlers: {
    open: () => void;
    error: (err: Error) => void;
    next: (event: MutationEvent) => void;
}): Subscription;
