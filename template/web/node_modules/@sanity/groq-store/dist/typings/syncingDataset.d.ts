import { SanityDocument } from '@sanity/types';
import { Config, EnvImplementations, Subscription } from './types';
export declare function getSyncingDataset(config: Config, onNotifyUpdate: (docs: SanityDocument[]) => void, { getDocuments, EventSource }: EnvImplementations): Subscription & {
    loaded: Promise<void>;
};
