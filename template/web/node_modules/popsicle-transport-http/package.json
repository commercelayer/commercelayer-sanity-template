{
  "name": "popsicle-transport-http",
  "version": "1.1.2",
  "description": "Popsicle transport for sending requests over HTTP1 and HTTP2",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "prettier": "prettier --write",
    "lint": "tslint \"src/**/*.ts\" --project tsconfig.json",
    "format": "npm run prettier -- \"*.{md,yml,yaml}\" \"src/**/*.{js,jsx,ts,tsx}\"",
    "build": "rimraf dist && tsc",
    "specs": "HTTP2_PORT=7359 HTTPS_PORT=7358 PORT=7357 jest --coverage --detectOpenHandles",
    "prespecs": "PORT=7357 node scripts/http-server.js & echo $! > server.pid; HTTPS_PORT=7358 node scripts/https-server.js & echo $! > https-server.pid; HTTP2_PORT=7359 node scripts/http2-server.js & echo $! > http2-server.pid",
    "postspecs": "if [ -f server.pid ]; then kill -9 $(cat server.pid); rm server.pid; fi; if [ -f https-server.pid ]; then kill -9 $(cat https-server.pid); rm https-server.pid; fi; if [ -f http2-server.pid ]; then kill -9 $(cat http2-server.pid); rm http2-server.pid; fi",
    "test": "npm run lint && npm run build && npm run specs",
    "prepare": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/serviejs/popsicle-transport-http.git"
  },
  "keywords": [
    "popsicle",
    "transport",
    "http",
    "http2",
    "response"
  ],
  "author": {
    "name": "Blake Embrey",
    "email": "hello@blakeembrey.com",
    "url": "http://blakeembrey.me"
  },
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/serviejs/popsicle-transport-http/issues"
  },
  "homepage": "https://github.com/serviejs/popsicle-transport-http",
  "jest": {
    "roots": [
      "<rootDir>/src/"
    ],
    "transform": {
      "\\.tsx?$": "ts-jest"
    },
    "testRegex": "(/__tests__/.*|\\.(test|spec))\\.(tsx?|jsx?)$",
    "moduleFileExtensions": [
      "ts",
      "tsx",
      "js",
      "jsx",
      "json",
      "node"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,json,css,md,yml,yaml}": "npm run prettier"
  },
  "publishConfig": {
    "access": "public"
  },
  "peerDependencies": {
    "servie": "^4.2.0"
  },
  "devDependencies": {
    "@types/jest": "^26.0.0",
    "@types/node": "^14.0.13",
    "@types/pump": "^1.1.0",
    "express": "^4.17.1",
    "husky": "^4.2.5",
    "jest": "^26.0.1",
    "lint-staged": "^10.2.9",
    "prettier": "^2.0.5",
    "servie": "^4.3.3",
    "throwback": "^4.1.0",
    "ts-jest": "^26.1.0",
    "tslint": "^6.1.2",
    "tslint-config-prettier": "^1.18.0",
    "tslint-config-standard": "^9.0.0",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "make-error-cause": "^2.2.0"
  }
}
