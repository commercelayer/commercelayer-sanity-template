export interface ManifestError {
    path: (string | number)[];
    message: string;
}
export declare type Valid<Manifest> = {
    type: 'valid';
    manifest: Manifest;
};
export declare type Invalid = {
    type: 'invalid';
    errors: ManifestError[];
    manifest: unknown;
};
