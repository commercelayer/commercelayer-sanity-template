"use strict";
var build = require('./build');
var watch = require('./watch');
var generateLockFiles = require('./generateLockFiles');
var check = require('./check');
var migrate = require('./migrate');
module.exports = { migrate: migrate, build: build, watch: watch, generateLockFiles: generateLockFiles, check: check };
