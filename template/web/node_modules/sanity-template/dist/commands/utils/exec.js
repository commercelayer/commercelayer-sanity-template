"use strict";
var nodeExecFile = require('child_process').execFile;
var promisify = require('util').promisify;
var execFile = promisify(nodeExecFile);
exports.exec = function (file, args, options) {
    return execFile(file, args, options).then(function (_a) {
        var stderr = _a.stderr, stdout = _a.stdout;
        return stdout;
    });
};
