"use strict";
var exec = require('./exec').exec;
var resolveBin = require('resolve-bin');
var npmbinary = resolveBin.sync('npm');
exports.runNpmInstall = function (directory) {
    return exec(npmbinary, ['install', '--prefix', directory, '--ignore-scripts', '--prefer-offline'], {
        cwd: directory
    });
};
