/// <reference types="node" />
import { IncomingHttpHeaders, IncomingMessage } from "http";
export declare function requestUrl(url: string, method: string, headers: IncomingHttpHeaders, postData: string): Promise<{
    res: IncomingMessage;
    body: string;
}>;
