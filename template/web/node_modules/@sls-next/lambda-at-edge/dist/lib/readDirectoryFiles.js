"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const klaw_1 = __importDefault(require("klaw"));
const fs_extra_1 = __importDefault(require("fs-extra"));
const readDirectoryFiles = (directory) => __awaiter(void 0, void 0, void 0, function* () {
    const directoryExists = yield fs_extra_1.default.pathExists(directory);
    if (!directoryExists) {
        return Promise.resolve([]);
    }
    const items = [];
    return new Promise((resolve, reject) => {
        klaw_1.default(directory.trim())
            .on("data", (item) => items.push(item))
            .on("end", () => {
            resolve(items);
        })
            .on("error", reject);
    });
});
exports.default = readDirectoryFiles;
