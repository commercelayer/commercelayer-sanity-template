import { CloudFrontRequest, CloudFrontResultResponse } from "aws-lambda";
import { OriginRequestEvent, OriginResponseEvent } from "../types";
export declare const handler: (event: OriginRequestEvent | OriginResponseEvent) => Promise<CloudFrontResultResponse | CloudFrontRequest>;
