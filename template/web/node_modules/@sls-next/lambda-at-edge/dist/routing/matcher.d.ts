import { Match } from "path-to-regexp";
export declare function matchPath(path: string, source: string): Match;
export declare function compileDestination(destination: string, params: object): string | null;
