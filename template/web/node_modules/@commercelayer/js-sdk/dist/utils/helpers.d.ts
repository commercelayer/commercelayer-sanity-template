export declare const cleanUrl: (url: string) => string;
export declare const parserParams: <P extends Partial<{
    filter: {
        q: object;
    };
    fields: object;
    include: string;
    page: {
        size: number;
        number: number;
    };
}>>(params: P) => P;
